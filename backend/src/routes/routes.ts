import {Router} from 'express';
//import {openRoom} from '../socket';

export const routes = Router();


//criar Sala//
//routes.get('/', openRoom)
//routes.post('/criarsala', criarSala)
//routes.post('/room', entrarEmSala);